<main class="container">
  <%= link_to sanitize("&larr; Back to project"), project_path(@project) %>

  <div class="header">
    <h1>Edit project</h1>
  </div>

  <%= turbo_frame_tag @project do %>
    <%= render "form", project: @project %>
  <% end %>
</main>
